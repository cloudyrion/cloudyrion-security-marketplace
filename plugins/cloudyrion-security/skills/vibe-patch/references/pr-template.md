# PR Template for Security Vibe Patch

## Title format
```
fix(security): Vibe Security Patch — N findings remediated (YYYYMMDD)
```

## Body template

Fill in all `<placeholders>` from the patch log.

---

```markdown
## Summary

This PR was generated by the Security team using the **Vibe Security Patching** methodology.
It remediates findings from the security code review conducted on <report date> against
commit `<report commit hash>`.

**Source report:** `security-code-review-report-<date>.md`
**Patch report:** `security-vibe-patch-report-<date>.md`
**Patch branch:** `security/vibe-patch-<date>`
**Base branch:** `<base branch>`

| Metric                                  | Value |
|-----------------------------------------|-------|
| Findings reviewed                       | N     |
| Patches applied                         | N     |
| Skipped — low confidence (manual req.)  | N     |
| Skipped — context changed               | N     |
| Semgrep verified                        | N     |

---

## ✅ Patched Findings

<!-- One entry per patched finding -->

### FINDING-NNN — <Title>
- **Severity:** [BLOCK]
- **CWE:** CWE-<id> — <n>
- **Location:** `<file>:<line>`
- **Semgrep rule:** `<rule-id>`
- **Fix:** <one sentence — what was changed>
- **Verification:** Semgrep confirmed clean | Semgrep skipped

---

## ⚠️ Manual Remediation Required

These findings were skipped because the fix requires business logic understanding.
No speculative patches were generated.

| Finding     | Location               | CWE     | Reason                    |
|-------------|------------------------|---------|---------------------------|
| FINDING-NNN | `<file>:<line>`        | CWE-NNN | <specific reason>         |

**Action required:** The code owner must implement these fixes. See patch report §4
for detailed guidance per finding.

---

## ⏭️ Skipped — Context Changed

| Finding     | Original Location      | Reason                   |
|-------------|------------------------|--------------------------|
| FINDING-NNN | `<file>:<line>`        | File deleted / lines shifted |

These require a fresh security-code-review scan.

---

## Verification

```bash
git checkout security/vibe-patch-<date>
semgrep scan --config=p/default <patched files list>
# Expected: zero findings for remediated rules in listed files
```

---

## ⚠️ Disclaimer

> This is a recommendation patch from the Security team. An engineer who owns and
> understands this code **must review it before merging**. Security patches preserve
> business logic to the best of our ability, but only the code owner can confirm
> correctness.

---
*Generated by security-vibe-patch on <YYYY-MM-DD HH:MM>*
*Author: <Firstname Lastname (email)>*
```
